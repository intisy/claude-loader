{
  "name": "claude-loader",
  "version": "1.1.1",
  "description": "TUI launcher and cc shell command for Claude Code.",
  "main": "dist/plugin.js",
  "type": "module",
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intisy/claude-loader.git"
  },
  "keywords": [
    "claude",
    "claude-code",
    "plugin",
    "launcher",
    "tui"
  ],
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npx tsc --project core/tsconfig.json && npx tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "plugin-updater": "^1.0.21",
    "typescript": "^6.0.3"
  }
}
